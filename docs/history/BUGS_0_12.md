# Bugs ODE 0.12

## Critical

1. `release/ODE_windows_test.zip` отсутствует. — Fixed.
   - Влияние: нельзя выполнить обязательную проверку release ZIP и нельзя
     считать ODE 0.12 готовой к переносу на рабочий ноутбук.
   - Исправление: ZIP пересобран из текущей проверенной версии проекта.
   - Проверка: `unzip -t` OK, SHA-256
     `993f127674baa95b8179901a9f4dd3bfdfa83e471b407221e5d8ddda6c332eaf`,
     БД внутри ZIP проходит `PRAGMA integrity_check = ok`.

## High

Нет найденных High-дефектов.

## Medium

Нет найденных Medium-дефектов.

## Low

1. Запуск приложения меняет SHA-256 рабочей SQLite-БД без бизнес-операций.
   - Наблюдение: изменились только значения `sqlite_sequence`; схема, основные
     счетчики и audit_log не изменились.
   - Влияние: контрольная сумма файла БД меняется после запуска, что усложняет
     строгую проверку неизменности файла.
   - Статус: known behavior, если бизнес-данные не меняются.
